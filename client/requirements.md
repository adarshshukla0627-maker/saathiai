## Packages
react-markdown | Rendering formatted markdown for chat messages
remark-gfm | GitHub flavored markdown for tables/lists in chat
date-fns | Formatting conversation timestamps

## Notes
The application expects SSE streaming at POST /api/conversations/:id/messages
